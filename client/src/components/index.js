// now for exporting all the components from the components folder
// we will be using the index.js

import Card from "./Card";
import FormField from "./FormField";
import Loader from "./Loader";

//now we are exporting everything as one object
export {Card,FormField,Loader}